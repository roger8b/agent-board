# SQLite database location used by the Prisma CLI.
# At runtime the app/CLI derive this from your home dir automatically
# (~/.agent-kanban/data.db) — this file is only needed for Prisma CLI commands.
DATABASE_URL="file:/absolute/path/to/your/home/.agent-kanban/data.db"
